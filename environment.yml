name: cliche
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - pip>=23.0
  - aiofiles>=22.1.0
  - beautifulsoup4>=4.11.0
  - requests>=2.27.0
  - numpy>=1.22.0
  - click>=8.0.0
  - psutil>=5.9.0
  - black>=23.0.0
  - pytest>=7.0.0
  - rich>=13.0.0
  - pip:
    - crawl4ai==0.4.248
    - litellm>=0.1.79
    - openai>=1.0.0
    - anthropic>=0.7.0
    - google-generativeai>=0.3.0
    - py3nvml>=0.2.7
    - pytest-asyncio>=0.21.0
    - html2text>=2024.2.26
    - mdformat>=0.7.0
    - art>=6.0
    - pydantic>=2.0.0
    - python-dotenv>=1.0.0
    - asyncio>=3.4.3
    - python-unsplash>=1.1.0
    - duckduckgo-search>=3.8.0
    - praw>=7.8.1
# System dependencies (not handled by conda):
# chafa - terminal image viewer (recommended version 1.12.0+)
# Install with: 
#   Ubuntu/Debian: sudo add-apt-repository ppa:hpjansson/chafa && sudo apt update && sudo apt install chafa
#   macOS: brew install chafa
#   Fedora/RHEL: sudo dnf copr enable hpjansson/chafa && sudo dnf install chafa
